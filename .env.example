# AI Research Portal — Environment Variables
#
# Copy this file to .env and fill in values for the providers you use.
# The local MLX provider does not require any API keys.

# OpenAI (required only when --provider openai)
# OPENAI_API_KEY=sk-...

# Anthropic (required only when --provider anthropic)
# ANTHROPIC_API_KEY=sk-ant-...
